# OSI Model Communication Demonstration
# Core dependencies

pyserial>=3.5          # Serial communication
colorama>=0.4.6        # Colored terminal output
tabulate>=0.9.0        # Table formatting for layer visualization

# Optional: For actual network testing
# scapy>=2.5.0         # Packet manipulation (requires root)
